import View from "components/ui/box/View";
import React from "react";

export default function PropertySectionContainer({ children }) {
  return <View className="property-section-container">{children}</View>;
}
